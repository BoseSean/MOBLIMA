package org.gp3.moblima.model;

public class Slot extends Model implements Comparable{
    public Slot() {}

    @Override
    public int compareTo(Object o) {
        return 0;
        // to be modified
        // earlier slot time should be smaller
        // will be used during sorting
    }
}
